/*"## Módulo 1

1. Escreva um programa que exiba Olá, mundo! no console.
2. Escreva um programa que calcule a média de três números e exiba o resultado no console.
    - Para calcular a média você só precisa somar os 3 números e dividir por 3.
3. Escreva um programa que verifique se um número é maior que 10 e exiba o resultado no console.
4. Escreva um programa que verifique se um número é positivo e exiba o resultado no console.
5. Escreva um programa que calcule a soma de dois números e exiba o resultado utilizando uma template string.
 Por exemplo, se os números forem 5 e 3, o programa deve exibir A soma de 5 e 3 é igual a 8"

*/

/* console.log("Hello world") */

/*
const numbers = {
    numero1: 5,
    numero2: 10,
    numero3: 15,
}
const media = (numbers.numero1 + numbers.numero2 + numbers.numero3) / 3

console.log(media) */

/*
const numbers = {
    numero1: 5,
    numero2: 10,
    numero3: 15,
}
function maiorque10(num) {
    if (num > 10) {
        return "Maior que 10";
    } else {
        return "Menor ou igual a 10";
    }
}
console.log(maiorque10(numbers.numero1))*/

/*
const numbers = {
    numero1: 5,
    numero2: -10,
    numero3: 0
}
function positivo(num) {
    if (num > 0) {
        return "positivo";
    } else if (num === 0) {
        return "neutro";
    } else {
        return "negativo";
    }
}
console.log(positivo(numbers.numero1))
console.log(positivo(numbers.numero2))
console.log(positivo(numbers.numero3)) */


/*
const numbers = {
    numero1: 5,
    numero2: 10,
}

function soma() {
    return numbers.numero1 + numbers.numero2;
}

const myString = `A soma de ${numbers.numero1} e ${numbers.numero2} é igual a ${soma()}`;


console.log(myString)*/