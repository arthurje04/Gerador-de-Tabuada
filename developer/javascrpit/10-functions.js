/* 
FUNÇÕES

As funções são blocos de código que realizam uma tarefa específica. Elas podem receber entradas (parâmetros) e podem retornar um valor.

Para declarar uma função em JavaScript, usamos a palavra-chave "function", seguida pelo nome da função e parênteses. O código da função é colocado entre chaves.

Exemplo:

function nomeDaFuncao(param1, param2) {
    // código a ser executado
    return resultado;
}

Para chamar uma função, usamos o nome da função seguido por parênteses, passando os argumentos necessários.

Exemplo:

let resultado = nomeDaFuncao(valor1, valor2);

so funcionam quando forem chamadas e nao pela ordem do codigo 

*/


function NNomeNaTela(){
    console.log("Rodolfo")
}